# Required Secrets in .env
- MONGODB_URI
    - the database uri from mongodb
- GOOGLE_ID
    - the oauth client id for using the service
- GOOGLE_CLIENT_SECRET
    - the oauth client secret for the app to access the client
- NEXTAUTH_SECRET
    - the hash required for hashing the jwt of the email of the provider